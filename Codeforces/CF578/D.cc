#include <bits/stdc++.h>

#define LL long long
#define REP(i, a) REPP(i, 0, (a) - 1)
#define REPP(i, a, b) for (int i = int(a); i <= int(b); i++)

using namespace std;

const int MO = 1e9 + 7;
const int N = 1e5 + 5;
int dp[N][16];

int main() {
#ifdef HOME
    freopen("in", "r", stdin);
#endif

    string s;
    cin >> s;
    int n = s.size();
    s += '#';






    return 0;
}

