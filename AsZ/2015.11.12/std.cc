#include <bits/stdc++.h>

#define LL long long
#define REP(i, a) REPP(i, 0, (a) - 1)
#define REPP(i, a, b) for (int i = int(a); i <= int(b); i++)

using namespace std;

set<string> s;

int main() {
#ifdef HOME
    freopen("my.out", "r", stdin);
#endif

    string s;
    while (getline(cin, s)) {
        cout << s << endl;
    }


    return 0;
}

